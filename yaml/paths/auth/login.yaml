post:
  description: Login
  operationId: Login
  tags:
    - Auth
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../../schemas/auth/login.yaml'
  responses:
    '200':
      description: Logged
      content:
        application/json:
          schema:
            $ref: '../../schemas/auth/tokenResponse.yaml'
    '400':
      description: Bad request